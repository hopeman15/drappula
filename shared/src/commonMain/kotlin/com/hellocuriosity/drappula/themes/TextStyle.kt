package com.hellocuriosity.drappula.themes

/**
 * Platform-agnostic text style identifiers.
 * Each platform provides its own font family, sizes, and weights for these styles.
 */
enum class TextStyle {
    DISPLAY,
    HEADLINE,
    TITLE,
    BODY,
    BUTTON,
    CAPTION,
}
